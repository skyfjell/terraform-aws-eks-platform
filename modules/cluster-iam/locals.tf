locals {
  user_arns            = var.user_arns
  name                 = var.name
  labels               = var.labels
  max_session_duration = var.max_session_duration
  actions              = var.actions
  cluster_arn          = var.cluster_arn
  attach               = var.attach
}
